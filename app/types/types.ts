export type ImgType = {
  description: string;
  img: string;
  bgColor?: string;
}[];

export type Type = {
  description: string;
  object: string;
  bgColor?: string;
}[];

export type TitleCardProps = {
  title: string;
};

export type UrlType = {
  url: string;
};

export type TechStackCardsPropsType = {
  title: string;
  skills: string;
  list: string[];
};
